<%= calendar_for(year.to_i, month.to_i, opts) do |day|
  if @linked_dates.present? and @linked_dates.include? day
    link_to(day.day, day_path(channel, day.year, day.month, day.day))
  else
    day.day
  end
end %>